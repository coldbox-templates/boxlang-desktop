# LTS Flow tests and formats ONLY automatically
# They are released manually via the `release` workflow
name: LTS Flow

on:
  push:
    branches:
      - "v*"

jobs:
  #############################################
  # Tests First baby! We fail, no build :(
  #############################################
  tests:
    uses: ./.github/workflows/tests.yml
    secrets: inherit